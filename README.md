# ai-program-dashboard
A web application that ingests mock project/operational data and uses the Claude API to automatically generate risk summaries, status narratives, and anomaly flags — then displays them in an interactive dashboard. Think: the tool a PM wishes they had to auto-write their weekly stakeholder update.
